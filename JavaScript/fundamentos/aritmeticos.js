const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d  // Sintaxe em fixed
const subtracao = d - b
const multiplicacao = a * b // Operadores binários
const divisao = d / a
const modulo = a % 2 // Se = 0 é par, se = 1 é ímpar

console.log(soma, subtracao, multiplicacao, divisao, modulo)

